gunit bashast;

start:
"echo \"finding file\"
for each in `ls | grep output`; do
echo \"I found a file\"
done

echo \"finding complete\"
"-> (LIST (echo "finding file") (for each (COMMAND_SUB (| ls (grep output))) (LIST (echo "I found a file"))) (echo "finding complete"))

"function quit {
  exit
}
hello () {
  echo Hello!
}
hello
quit
echo foo" -> (LIST (function quit (CURRSHELL (LIST exit))) (function hello (CURRSHELL (LIST (echo Hello!)))) hello quit (echo foo))
